{{- if .Values.frontend.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "remedyiq.frontendServiceAccountName" . }}
  labels:
    {{- include "remedyiq.frontendLabels" . | nindent 4 }}
automountServiceAccountToken: true
{{- end }}
